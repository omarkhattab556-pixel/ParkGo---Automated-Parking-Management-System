# Loaded automatically by `vite build` (production mode).
# These are baked into the bundle at build time and are NOT secrets
# (the frontend is public anyway), so it is safe to commit them.
VITE_API_URL=https://parkgo-automated-parking-management.onrender.com/api
VITE_APP_NAME=ParkGo
VITE_TOTAL_PARKING_SPACES=50
